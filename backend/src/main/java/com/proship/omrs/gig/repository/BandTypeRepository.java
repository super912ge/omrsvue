package com.proship.omrs.gig.repository;

import com.proship.omrs.gig.entity.BandType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BandTypeRepository extends JpaRepository<BandType,Long>{
}
