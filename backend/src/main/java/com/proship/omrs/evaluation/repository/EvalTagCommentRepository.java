package com.proship.omrs.evaluation.repository;

import com.proship.omrs.evaluation.entity.EvalTagComment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EvalTagCommentRepository extends JpaRepository<EvalTagComment,Long>{
}
