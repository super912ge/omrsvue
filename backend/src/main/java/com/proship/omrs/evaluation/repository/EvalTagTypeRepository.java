package com.proship.omrs.evaluation.repository;

import com.proship.omrs.evaluation.entity.EvalTagType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EvalTagTypeRepository extends JpaRepository<EvalTagType,Long>{
}
