package com.proship.omrs.evaluation.repository;

import com.proship.omrs.evaluation.entity.EvalTagTypeDiscriminator;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EvalTagTypeDiscriminatorRepository extends JpaRepository<EvalTagTypeDiscriminator,Long>{
}
