package com.proship.omrs.venue.repository;

import com.proship.omrs.venue.entity.VenueBase;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VenueBaseRepository extends JpaRepository<VenueBase,Long>{
}
